fix(ci): replace hatch with uv run pytest; fix Windows subprocess hang

- Switch CI from `hatch run test` to `uv run --extra devel-test pytest
  tests/` to avoid virtualenv incompatibility with Python 3.8/3.9
  (virtualenv>=20.27.0 dropped propose_interpreters for older Pythons)
- Fix `test_run_default_is_silent` and `test_run_sets_env` passing bare
  `sys.executable` to subprocess, which waits on stdin in Windows CI;
  pass `-c ""` so Python exits immediately
