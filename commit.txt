fix(cli): catch ValueError from api at CLI boundary

Wraps the subcommand handler dispatch in a try/except ValueError so
that a "No env file found" raised by create_env or lint_env (e.g. if
the file disappears after the pre-check) produces a clean error message
and exit code 1 instead of an unhandled traceback.
