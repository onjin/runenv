fix(cli): print help instead of error when runenv is called with no subcommand

Bare `runenv` (no args, no legacy env file) now prints --help and exits 0
instead of crashing with "Unknown subcommand".
